# MCP Servers

Shared MCP server definitions and implementation notes.

Each server entry should include:

- Required runtime and minimum version.
- Install command for Windows, Linux, and macOS.
- Startup command.
- Required environment variables.
- Known OS limitations or filesystem assumptions.
