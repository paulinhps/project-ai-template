# MCP Configs

Shared MCP configuration files and examples.

Use portable examples by default:

- Prefer slash-form relative paths.
- Avoid user-profile paths such as `%APPDATA%`, `$HOME`, and `~/...` unless the same variable is documented for each supported OS.
- Keep secrets out of config examples; reference environment variables instead.
