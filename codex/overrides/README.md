# Codex Overrides

Codex-specific behavior and local overrides. Keep shared behavior in `AGENTS.md`, `.ai/rules`, and the shared folders first; add files here only for genuine Codex-specific behavior.
